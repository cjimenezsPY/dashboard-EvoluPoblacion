import { theme } from "./theme";

export const colors = {
  REGULARES: theme.colors.regular,
  AGRARIOS: theme.colors.agrario,
  POTESTATIVOS: theme.colors.potestativo,
  OTRAS: theme.colors.otras,
  TOTAL: theme.colors.total
};